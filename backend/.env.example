# Backend Server Configuration
PORT=4000

# Google Gemini API Key (Required for Simulation Engine)
GEMINI_API_KEY=your_gemini_api_key_here

# Live Cricket Scores API (Optional)
# If provided, the backend will fetch live data.
# If omitted, the backend runs in Demo Mode using the mock match simulator.
